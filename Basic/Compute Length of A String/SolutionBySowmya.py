# Defining function for calculating the length of the string.
def len_of_string(s):
   print(len(string))
# taking the input from the user
string=str(input("Enter string of any length:"))
#function calling:
len_of_string(string)
