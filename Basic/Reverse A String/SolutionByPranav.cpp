#include<bits/stdc++.h> 

using namespace std; 

int main() 
{ 
    string st; 
    cout<<"Input = ";
    getline(cin,st);
    reverse(st.begin(), st.end()); 
    cout<<"Output = "<<st;
    return 0; 
}
