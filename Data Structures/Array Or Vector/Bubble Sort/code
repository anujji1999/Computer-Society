#include <iostream>
using namespace std;
int* bubbleSort(int *arr,int n){
    for(int i=0;i<n;i++){
        for(int j=1;j<n-i;j++){
            if(arr[j]<arr[j-1]){
                int temp=arr[j];
                arr[j]=arr[j-1];
                arr[j-1]=temp;
                
            }

        }
    }
    return arr;
}
int main() {
   
   int n;
   cin>>n;
   int *arr=new int[n];
   for(int i=0;i<n;i++){
       cin>>arr[i];

   }
   arr=bubbleSort(arr,n);
   for(int i=0;i<n;i++){
       cout<<arr[i]<<", ";
   }
}
